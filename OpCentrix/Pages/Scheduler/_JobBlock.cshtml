@model OpCentrix.Models.Job

<div class="absolute top-1 left-1 right-1 bg-blue-600 text-white text-xs px-2 py-1 rounded shadow">
    <div class="font-semibold">@Model.PartNumber</div>
    <div class="text-[10px]">@Model.ScheduledStart:MMM d → @Model.ScheduledEnd:MMM d</div>
</div>
